<h1>Companies</h1>

<h1>Listing Companies</h1>

<table>
  <thead>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Instructions</th>
    <th>Title</th>
    <th>Description</th>
    <th>Instructions</th>
    <th>Title</th>
    <th>Description</th>

    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @companies.each do |company| %>
    <tr>
      <td><%= company.company_id %></td>
      <td><%= company.name %></td>
      <td><%= company.website %></td>
      <td><%= company.address %></td>
      <td><%= company.size %></td>
      <td><%= company.founded %></td>
      <td><%= company.revenue %></td>
      <td><%= company.synopsis %></td>
      <%=session[:role] %>
      <%=@realtor_company  %>
      <%= company.company_id%>
        <% if session[:role] == "admin" || (session[:role] == "realtor" && @realtor_company == company.company_id) %>
      <td> <%= link_to 'Edit', edit_company_path(company) %> </td>

      <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%end %>

    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add Companies', {:controller =>'companies', :action =>"new"} , :class => "btn btn-primary"%> <br/><br/>